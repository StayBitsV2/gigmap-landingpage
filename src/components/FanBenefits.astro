---
import type { languages } from "../i18n/ui";
import { useTranslations } from "../i18n/utils";
import CardsContainer from "./CardsContainer.astro";
import SectionDivider from "./SectionDivider.astro";

const currentLang = Astro.currentLocale || "en";
const t = useTranslations(currentLang as keyof typeof languages);

const benefits = [
  {
    body: `${t("beneficiofan1_parte1")} ${t("beneficiofan1_resaltado")} ${t("beneficiofan1_parte2")}`,
    icon: "fas fa-bell",
  },
  {
    body: `${t("beneficiofan2_parte1")}<br>${t("beneficiofan2_parte2")}`,
    icon: "fas fa-envelope",
  },
  {
    body: `${t("beneficiofan3_parte1")}<br>${t("beneficiofan3_resaltado")} ${t("beneficiofan3_parte2")}`,
    icon: "fas fa-user-friends",
  },
  {
    body: `${t("beneficiofan4_parte1")}<br>${t("beneficiofan4_parte2")}`,
    icon: "fas fa-map",
  },
];
---

<section class="pt-33">
  <CardsContainer
    cols={2}
    cards={benefits}
    title={`${t("titulo_beneficios_fan_parte1")} <span class="text-(--primary)">${t("titulo_beneficios_fan_resaltado")}</span> ${t("titulo_beneficios_fan_parte2")}`}
    variant="BENEFIT"
  />
</section>
<SectionDivider />