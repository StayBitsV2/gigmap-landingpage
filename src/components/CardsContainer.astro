---
import Card from "./Card.astro";
import SectionDivider from "./SectionDivider.astro";
import SectionTitle from "./SectionTitle.astro";

const { title, subtitle, cards, cols = 3, variant = "CONCERT" } = Astro.props;
---

<SectionDivider />
<section class="md:px-5">
  <SectionTitle title={title} subtitle={subtitle} />
  <div class="flex justify-center">
    <div
      class={`grid gap-15 mb-15 grid-cols-1 md:grid-cols-2 ${cols === 2 ? "" : "xl:grid-cols-3"} justify-center
        ${
          cards.length % 2 !== 0
            ? "md:[&>*:last-child]:col-span-2 md:[&>*:last-child]:justify-self-center lg:[&>*:last-child]:col-auto lg:[&>*:last-child]:justify-self-auto"
            : ""
        }`}
    >
      {
        cards.map((card: any) => (
          <Card
            img={card.img}
            btn={card.btn}
            icon={card.icon}
            body={card.body}
            footer={card.footer}
            variant={variant}
          />
        ))
      }
    </div>
  </div>
</section>
